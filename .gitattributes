# 저장소에는 항상 LF 로 정규화해 저장한다.
# eol 을 명시해 워킹트리 개행까지 고정한다 — 이렇게 하지 않으면 클론하는 쪽의
# core.autocrlf 설정에 따라 macOS 와 Windows 의 워킹트리가 서로 달라진다.
* text=auto eol=lf

# 셸 스크립트는 CRLF 로 변환되면 실행되지 않는다.
*.sh   text eol=lf
*.zsh  text eol=lf

# PowerShell 스크립트만 CRLF 를 유지한다.
*.ps1  text eol=crlf
